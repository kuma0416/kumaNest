<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>新增商品</title>
    <style>
        header {
            display: block;
            text-align: center;
            width: 100%;
            background-color: rgba(0, 0, 0, 0.6);
            color: white;
            position: fixed;
            top: 0;
            font-family: Arial;
            height: 60px;
            font-size:25px;
            width:100%;
            left: 0;
            padding-top:20px;
        }

        #listSpace {
            margin-top: 100px;
            width: 60%;
            float: left;
            height: 100%;
        }

        #Form {
            margin-top: 100px;
            float: left;
            width: 40%;
        }

        a:visited{
            text-decoration: none;
            color:#FF8800;
        }

        a:hover{
            background-color:#0044BB;
            color:white;
        }

    </style>
</head>
<body>
    <div>
    <header >
        <a href="/">Kuma窩</a>
    </header>
    </div>
    <div id="Form">
        <h2>新增商品</h2>
        <form id="login" method="POST" action="/addItem" enctype="multipart/form-data">
            <div>
                <fieldset style="width: 60%;">
                    商品名稱: <input type="text" name="itemName" required>
                    <br>
                    價格: <input type="text" name="itemPrice" required>
                    <br>
                    上傳圖片: <input id="imgUp" type="file" name="itemPic">
                    <img id="previewImg" src="#">
                    <input type="submit" value="新增">
                    <script>
                            $("imgUp").change(function(){
                                readURL(this);
                            });
                            function readURL(input) {
                                console.log(input);
                                if(input.files && input.files[0]) {
                                    var reader = new FileReader();
                    
                                    reader.onload = function(e){
                                        $('#previewImg').attr('src', e.target.result);
                                    }
                                    reader.readAsDataURL(input.files[0]);
                                }
                            }
                        </script>
                </fieldset>
            </div>
        </form>
    </div>

</body>
</html>